import json
import sys
import re

# Since I don't have direct access to the chat history as a file, 
# I can just look at the exact text. Wait, can I? I can't read the chat history file directly unless I write a script to fetch it or just copy-paste it from my context.

